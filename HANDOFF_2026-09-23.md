# 인수인계 — 2026-09-23 작업분

대상: 다음 세션 / 이 브랜치를 이어받는 작업자
기준: 브랜치 `feature/hhj-0923`, HEAD `2287853 (0923/1018)`

기존 문서 `HANDOFF_tray_detection.md` 는 **전체 구조·좌표계 설명용**으로 계속 유효하다.
이 문서는 **오늘 바꾼 것과 그 근거, 아직 안 끝난 것**만 다룬다.
단, 기존 문서 중 오늘 바뀌어 **틀리게 된 부분**이 있다 — 아래 [7. 기존 문서에서 낡은 부분](#7-기존-문서에서-낡은-부분) 을 먼저 볼 것.

---

## 0. 지금 당장 알아야 할 것

**Nav2 파라미터는 아직 빌드가 안 돼 있다.** 소스에는 새 값이 들어갔지만
`install/` 사본이 옛 값이라, 지금 그냥 실행하면 **주행 속도가 0.55 m/s 로 나온다.**

```bash
cd ~/cobot3_ws && colcon build --packages-select carter_navigation --symlink-install
```

`--symlink-install` 로 한 번 빌드해두면 이후 yaml 수정은 **Nav2 재시작만으로** 반영된다.
이걸 몰라서 오늘 두 번 헛돌았다 (자세한 경위는 [5-2](#5-2-주행-속도가-안-오르던-이유)).

> BT XML(`nav_through_poses_human_test.xml`)은 params 가 **절대경로로 `src/` 를 직접 가리켜서**
> 빌드 없이 Nav2 재시작만으로 반영된다. 팔 쪽(`pick_and_place_detection.py`, `detect_node.py`)도
> 소스를 직접 실행하므로 빌드와 무관하다. **빌드가 필요한 건 Nav2 params 뿐이다.**

---

## 1. 커밋 상태

| | 내용 |
|---|---|
| 커밋됨 `2287853` | 스텝 실패 시 건너뛰기 (`PickPlaceSequence.skip`) |
| **미커밋** | 나머지 전부 (파일 4개, +149/-79) |

```
 M admin_ws/src/tray_detector/tray_detector/detect_node.py
 M isaacpjt/M0609/teleop/pick_and_place_detection.py
 M src/nova_carter/carter_navigation/behavior_trees/nav_through_poses_human_test.xml
 M src/nova_carter/carter_navigation/params/carter_navigation_params_human_test.yaml
```

---

## 2. 오늘 바꾼 것 — 팔 (`pick_and_place_detection.py`)

| # | 변경 | 이유 |
|---|---|---|
| 1 | **스텝 실패 시 홈 복귀 → 그 스텝만 건너뛰고 계속** | 미션이 중간에 멈추지 않게. 사용자 지시 |
| 2 | `IK_ORIENTATION_TOLERANCE` 0.02 → **0.1745**(10°)<br>`STEP_ROT_TOL_DEG` 5.0 → **12.0** | 특이점 회피 시도. 두 값은 **후자가 커야** 한다 |
| 3 | **관절 후퇴(`joint_retreat_step`) 호출 제거** — 적재·하역 양쪽 | 회전 전 2단계 동작이 불필요해 보인다는 판단 |
| 4 | **관측 자세를 고정 관절값으로** (`OBSERVE_JOINTS_DEG`), `observe_tilt` 상태 삭제 | IK 가 목표에 도달 못 해 관측 자세가 매번 달라졌다 |
| 5 | **하역을 적재와 분리** — `UNLOAD_ABOVE_Z_M`(0.12) 신설, 왕복 스텝 제거 | 넣을 때와 뺄 때 조건이 다름 |
| 6 | **트레이 스폰을 가로 한 줄로**, 범위 ±0.25 | 파지점 도달거리를 특이 구간 밖으로 (아래 [5-1](#5-1-손목-특이점의-진짜-원인)) |
| 7 | **특이점 가드의 거부 제거**(경고 로그는 유지) | 막으면 팔이 제자리에 서고 스텝이 실패해서 |
| 8 | **`--drive-only` 플래그 추가** | 팔 없이 주행만 시험 |

### 되돌리는 법
- **2번**: `IK_ORIENTATION_TOLERANCE` 0.02, `STEP_ROT_TOL_DEG` 5.0 (둘 다 같이)
- **7번**: `SingularityGuardedIK.compute_inverse_kinematics` 의 `return action, solved` → `return action, False`
- **3번**: `build_pick_steps` / `build_unload_steps` 에 `joint_retreat_step(...)` 한 줄씩 복구.
  함수 본체와 `RETREAT_*` 상수는 **안 지우고 남겨뒀다** (현재 죽은 코드 약 40줄)

---

## 3. 오늘 바꾼 것 — 검출 (`detect_node.py`)

```
RACK_ROI  (160, 190, 480, 320)  →  (165, 175, 510, 255)
```
오른쪽 칸 마커가 x=480 경계에 잘리고, 아래 94px 은 마커 없는 트레이 몸통이었다.
마커 실측 중심 x = **225 / 338 / 451**(간격 113px)이라, 폭 345 의 3등분 중앙(222/337/452)에
칸마다 하나씩 오도록 맞췄다. **`slot_of()` 가 이 ROI 를 가로 3등분해 칸을 매기므로
폭과 위치를 바꾸면 칸 배정이 같이 움직인다.**

> ⚠️ ROI 를 옮겨도 **3번 칸 마커는 이 프레임에서 안 읽혔다.** 마커 영역만 2~12배로 확대해도
> 전부 0개. 잘림 외의 요인(각도/번짐)이 있다. `ARUCO_ZOOM=3` 이 우연히 최적이고,
> 배율을 바꾸면 오히려 더 안 읽힌다(2→1개, 4→1개, 8→1개). 판독은 여전히 불안정하고
> `OBSERVE_COLLECT_FRAMES`(90프레임 누적)에 의존하는 구조다.

---

## 4. 오늘 바꾼 것 — 주행

### 4-1. `carter_navigation_params_human_test.yaml`
```yaml
max_vel_x / max_speed_xy / velocity_smoother.max_velocity[0]   0.5 → 1.5
acc_lim_x / velocity_smoother.max_accel[0]                     0.5 → 2.0
decel_lim_x / velocity_smoother.max_decel[0]                  -0.5 → -2.0
forward_prune_distance                                         1.0 → 3.0
```
**회전축(theta)은 건드리지 않았다.** `max_vel_theta` 0.5, `acc_lim_theta` 1.0 그대로.
곡선 속도는 `v = ω × R` 로 자동 제한된다(반경 3m 미만이면 1.5 보다 느려짐) — 별도 설정 불필요.

**반드시 같이 움직여야 하는 짝** (어긋나면 DWB 계산과 실제 제동이 달라져 도킹 오버슈트):
```
FollowPath.acc_lim_x      ↔ velocity_smoother.max_accel[0]
FollowPath.decel_lim_x    ↔ velocity_smoother.max_decel[0]
FollowPath.acc_lim_theta  ↔ velocity_smoother.max_accel[2]
FollowPath.decel_lim_theta↔ velocity_smoother.max_decel[2]
behavior_server.rotational_acc_lim ↔ FollowPath.acc_lim_theta
behavior_server.max_rotational_vel ↔ FollowPath.max_vel_theta
```

**단계적으로 낮출 때** (속도 / 가감속 / 정지거리):
```
1.5 / 2.0 / 0.56m   ← 현재     1.2 / 1.5 / 0.48m
1.0 / 1.0 / 0.50m              0.7 / 0.5 / 0.49m
```
속도와 가감속을 같이 움직여야 정지거리가 유지된다.

### 4-2. `nav_through_poses_human_test.xml`
```
RateController hz  1.0 → 3.0
RemovePassedGoals radius  0.7 (유지)
```
속도를 3배로 올리면서 필수로 따라온 변경이다. 이유는 [5-3](#5-3-경유지-통과-조건).

---

## 5. 오늘 밝혀낸 것 (측정값)

### 5-1. 손목 특이점의 진짜 원인
`joint_5 = 툴 pitch − joint_2 − joint_3` 이고, `grasp_frame()` 이 툴 기울기를
`POINT2_RPY` 로 **고정**하므로 **`joint_5` 는 도달거리로 결정된다.**
URDF + Lula 로 오프라인 IK 를 풀어 측정(σ_min = 자코비안 최소 특이값, 0 이면 특이):

| 도달거리 | joint_5 | σ_min | |
|---|---|---|---|
| 0.80 m | −60.7° | 0.248 | 최적 |
| 0.95 m | −42.9° | 0.194 | 안전 |
| 1.05 m | −27.0° | 0.126 | 안전 |
| 1.10 m | −13.7° | 0.061 | 주의 |
| 1.12 m | 발산 | **0.000** | **도달 불가** |

실패했던 `들어올리기` 자세는 **σ_min 0.033**. 파지점은 검출면보다
`TRAY_HALF_DEPTH_M`(5.7cm) 더 깊어서, 반원 스폰의 먼 쪽(1.03m)이 실제로는 **1.09m** 지령이었다.
→ 가로 한 줄 스폰으로 바꿔 파지점을 **0.91~0.94m** 에 모았다.

**`GRASP_REACH_MAX_M = 1.12` 는 작업영역 경계 밖이다** (이력 0.95 → 1.05 → 1.12).
스폰을 바꿔 당장은 안 걸리지만, 값 자체는 손대지 않았으니 재검토 대상.

> **`joint_4` 를 돌려서는 못 고친다.** `joint_5` 를 −4.4° 로 고정하고 `joint_4` 를 360°
> 돌려봐도 σ_min 이 0.018~0.035 사이에서만 움직인다. 반면 `joint_5` 를 0 에서
> 20° 만 떼어내면 σ_min 이 5배가 된다. 로그에서 튀는 건 `joint_4` 지만
> **원인 변수는 `joint_5`** 다 (j5≈0 이면 j4/j6 축이 겹쳐 값이 미결정 상태가 된다).

### 5-2. 주행 속도가 안 오르던 이유
두 단계로 막혀 있었다.

1. **`install/` 사본이 옛 값** — launch 가 `get_package_share_directory()` 로 경로를 잡아서
   소스를 고쳐도 빌드 전엔 반영 안 됨. `v=0.50` 이 정확히 찍힌 게 증거였다.
2. **`forward_prune_distance` 가 순항 속도를 정한다** —
   ```
   v ≈ forward_prune_distance / sim_time
   1.0 / 1.7 = 0.588 m/s
   ```
   DWB 속도 샘플 간격은 `max_vel_x / (vx_samples−1)` = 1.5/19 = **0.0789**.
   0.588 아래 최대 샘플 = 7번 = **0.553** → 로그의 `v=0.55` 와 정확히 일치.
   (딱 한 번 나온 0.63 은 8번 샘플 0.632)

   `max_vel_x` 가 0.5 였을 땐 이 자연 속도(0.588)보다 상한이 낮아 **0.50 에 붙어 있었다.**
   두 관측이 같은 원인이었다.

**`ros2 param set` 은 프로세스 메모리에만 남는다** — Nav2 재시작하면 사라지고 설치본 값으로 돌아간다.

### 5-3. 경유지 통과 조건
`RemovePassedGoals` 는 **기억 없는 스냅샷 검사**다. tick 하는 순간 반경 안에 있는지만 본다.

```
필요 radius:  R ≥ √( e² + (v/hz/2)² )
   e  = 경유지 최근접 거리(코너 절삭량)
   v/hz = tick 간 이동거리
```

| 속도 | hz | d/2 | |
|---|---|---|---|
| 0.5 | 1.0 | 0.25 | 검증된 조합 |
| **1.5** | **1.0** | **0.75** | ★ radius 0.7 초과 — 원래 실패 조건과 동일 |
| 1.5 | 2.0 | 0.38 | 여유 얇음 |
| **1.5** | **3.0** | **0.25** | 검증된 조합과 동일 → **채택** |

> XML 주석의 *"radius 1.2 로 올렸다 되돌림 (통과폭 1.98m < 회전 지름 2.11m)"* 실측은
> **지금은 삭제된 경유지 `(-1.937, 3.416)` 기준**이다. 거리 1.16m/0.70m 가 센티미터까지 일치함을
> 확인했다. 현재 경유지 4개와는 무관하므로, **radius 는 이제 올릴 수 있는 값이다.**
> `hz` 만으로 부족하면(= `e` 가 0.65m 초과) radius 0.9~1.2 도 선택지다.

### 5-4. Lula IK 의 한계
**축별(per-axis) 구속을 지원하지 않는다.** 설치본을 직접 확인:
- `compute_ik_ccd(kinematics, target_pose: Pose3, frame, config)` — 목표는 포즈 통째
- `CyclicCoordDescentIkConfig` 의 자세 관련 속성은 **스칼라 3개뿐**
  (`orientation_tolerance`, `ccd_orientation_weight`, `bfgs_orientation_weight`)

쓸 수 있는 건 **6자유도 완전 구속** 또는 **위치만(3자유도, `target_orientation=None`)** 두 가지.
그 사이는 등방(isotropic) 완화뿐이고 **어느 축이 희생될지 제어 못 한다.**
5-DOF(yaw 자유)를 흉내내려면 yaw 후보를 여러 개 풀어 고르는 방식밖에 없다.

---

## 6. 미해결 / 검증 대기

| 항목 | 상태 |
|---|---|
| **Nav2 params 빌드** | ❗ 안 됨 — [0번](#0-지금-당장-알아야-할-것) |
| **경유지 통과 정확도** | 속도 1.5 에서 미검증. 리포트의 `최근접` 이 **0.65m 초과면** 대책 필요 |
| **트레이 기울기** | IK tolerance 10° → 최대 10° 기운 채로도 성공 판정. 시험관 육안 확인 필요 |
| **특이점 가드 해제 영향** | `joint_4` 89°/프레임 지령이 실제로 나간다. 경고가 뜨면 손목이 휘둘릴 수 있음 |
| **관절 후퇴 제거 영향** | 적재: 뻗은 채 +86° 회전 → 옆 트레이 쓸림 가능<br>하역: 슬롯 위 12cm 에서 회전 → 책상 위 트레이 접촉 가능 |
| **3번 칸 ArUco** | ROI 옮겨도 미검출. 원인 미규명 |
| **죽은 코드** | `joint_retreat_step`, `tcp_pose_base`, `RETREAT_*` 상수 (약 40줄) |
| **`collision_monitor`** | `min_points: 6000000000` 이라 **사실상 꺼져 있다.** 속도 3배 상황에서 독립 안전층 없음 |
| **`GRASP_REACH_MAX_M`** | 1.12 = 작업영역 밖. 스폰 변경으로 우회했을 뿐 |

---

## 7. 기존 문서에서 낡은 부분

`HANDOFF_tray_detection.md` 에서 **지금 코드와 다른** 항목:

- **§3-1 실행** — `--drive-only` 모드가 추가됐다 (아래 [8번](#8-실행))
- **§4 동작 시퀀스** — `observe_tilt` 상태가 **없어졌다**. 관측은 고정 관절값 1스텝
- **§7 "스텝이 실패해도 미션은 안 멈춘다"** — 2)·3)·4) 항이 스텝 실패에 더는 해당 안 됨.
  이제 **그 스텝만 건너뛰고 계속**한다. `build_return_steps()` 는 호출부가 사라져 죽은 코드
- **§7 "손목 특이점 — 회전 전 후퇴는 관절 공간으로"** — 그 후퇴 자체를 제거했다
- **§6 튜닝 상수** — `RACK_ABOVE_Z_M`(적재 전용), `UNLOAD_ABOVE_Z_M` 신설, `TRAY_SPREAD_R_M` 0.25

> 기존 문서를 직접 고치지 않은 이유: 오늘 변경 상당수가 **실험 중**이라 되돌릴 수 있다.
> 값이 확정되면 기존 문서에 합치는 게 맞다.

---

## 8. 실행

환경은 기존 문서 §3-0 과 동일 (매 터미널 `source /opt/ros/jazzy/setup.bash` 필수).

### 전체 시나리오 (터미널 4개, A → B에서 Play → C → D)
기존 문서 §3-1 과 같다.

### 주행만 시험 (팔 없음) — 오늘 추가
**관제 노드(A)는 띄울 필요 없다.** Isaac + Nav2 + 주행 미션 3개만.

```bash
# B. Isaac (Play 누르면 바로 /mission_state 1 발행)
cd ~/cobot3_ws && source /opt/ros/jazzy/setup.bash && \
  ~/isaacsim/python.sh isaacpjt/M0609/teleop/pick_and_place_detection.py --drive-only

# C. Nav2
cd ~/cobot3_ws && source /opt/ros/jazzy/setup.bash && source install/setup.bash && \
  ros2 launch carter_navigation nav2_human_test.launch.py

# D. 주행 미션
cd ~/cobot3_ws && source /opt/ros/jazzy/setup.bash && source install/setup.bash && \
  ros2 run nav_to_goal through_pose_human_test
```

반복할 때는 **Isaac 에서 Stop → Play**. Stop 이 `/mission_state 0` 발행과 `/nav_done` 초기화를
하므로, Play 만 다시 누르면 이전 주행의 `nav_done` 이 남아 바로 끝나버린다. D 도 매번 재실행.

### 속도 관련 진단
```bash
ros2 topic echo /cmd_vel_nav --field linear.x   # DWB 원 출력 (스무더 이전)
ros2 topic hz /cmd_vel                          # 20Hz 나오는지
ros2 param set /controller_server FollowPath.forward_prune_distance 3.0   # 재시작 시 사라짐
```
주행 미션이 끝에 찍는 `[경유지 통과 정확도]` 리포트의 `최근접` 이 [5-3](#5-3-경유지-통과-조건)의 `e` 다.
